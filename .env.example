# Which AI provider the app uses. Defaults to "mock" so the app works with no keys.
# Add your own providers in lib/ai/providers/ and register them in lib/ai/provider.ts,
# then set this to the provider's name.
AI_PROVIDER=mock
